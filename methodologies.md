# Список техник и методологий: Интроспекция и Мультиагентная Оркестрация

На основе последних исследований arXiv (2024–2025) сформирован следующий перечень ключевых методологий для работы с LLM-агентами.

---

## 1. Техники интроспекции и рассуждения

### 🧠 Шаблоны мыслей (Thought Templates)

* **Суть:** Использование жестко структурированных шаблонов для "внутреннего монолога" модели. Вместо свободного Chain-of-Thought (CoT) модель обязана следовать этапам: `Анализ` -> `Поиск противоречий` -> `Верификация` -> `Решение`.
* **Пример промпта:**
    > "Перед ответом заполни блок <thought_process>. Обязательно выдели подсекции: 1. Понимание задачи, 2. Потенциальные ловушки, 3. План шагов."
* **Источник:** *SuperCorrect (2024)*.

### 🔄 Диффузионный CoT (DiffCoT)

* **Суть:** Итеративное "размытие" и последующее уточнение цепочки рассуждений. Агент не просто генерирует один CoT, а постоянно улучшает его, убирая "шум" и логические ошибки на ранних этапах.
* **Пример:** Агент генерирует черновик рассуждения, затем второй агент (или тот же в другом промпте) ищет слабые места, и процесс повторяется до стабилизации логики.
* **Источник:** *DiffCoT (2026)*.

### 🛠 Самокоррекция (Self-Correction / ReasonFlux)

* **Суть:** Механизм, при котором модель принудительно "оглядывается" на свой предыдущий токен или блок текста.
* **Методология:** Внедрение этапа "Критик", который срабатывает после каждого значимого вывода агента, проверяя его на соответствие фактам или логике.

---

## 2. Мультиагентная оркестрация и работа в команде

### 🤝 Коллаборативный дизайн (GenAI Collaboration)

* **Суть:** Разделение ролей на "Исполнителей" и "Архитекторов". Оркестратор не просто передает данные, а управляет иерархией доступа к контексту.
* **Методология:** Использование "Контекстного инжиниринга" (Context Engineering) для того, чтобы каждый агент видел только ту часть задачи, которая необходима для его роли.
* **Источник:** *Towards Effective GenAI Multi-Agent Collaboration (2024)*.

### 📡 Динамическая маршрутизация (Dynamic Routing)

* **Суть:** Оркестрация, в которой путь выполнения задачи не задан жестко (DAG), а определяется "Диспетчером" на лету в зависимости от сложности промежуточного результата.

---

## 3. Промпт-инжиниринг для оркестрации

### 📝 Инструктивные шаблоны (Instructional Templates)

* **Методика:** Создание модульных промптов, которые можно комбинировать. Вместо одного огромного системного промпта используются микро-инструкции для конкретных состояний агента.
* **Пример:** `System_Base` + `Module_Introspection` + `Module_Code_Rules`.

---

## Как это применить сейчас?

Для вашего проекта я рекомендую внедрить **Thought Templates** в системный промпт базового агента. Это обеспечит ту самую "интроспекцию", о которой вы спрашивали.
